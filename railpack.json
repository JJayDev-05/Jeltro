{
  "providers": ["php"],
  "packages": {
    "php": "8.4.*"
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan migrate --force && frankenphp run --config /etc/caddy/Caddyfile"
  }
}
